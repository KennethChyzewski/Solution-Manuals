class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        if not s:
            return 0
        words = s.split()
        if not words:
            return 0
        return len(words[-1])
